#================================================================
# Jenkins config for jenkins.sh
# Save as: ./jenkins.config (same directory as jenkins.sh)
# Permissions: chmod 600 ./jenkins.config   # recommended
# This file is "sourced" by Bash—ONLY set variables here.
#================================================================

# ----------------------------------------------------------------
# Admin account for Jenkins (created by the Groovy init script)
# NOTE on quoting:
# - If your password contains a single quote ('), wrap the value in double quotes.
# - If it contains a double quote ("), wrap the value in single quotes.
# - Backslashes should be escaped as \\.
# ----------------------------------------------------------------
admin_user='admin'
admin_password='ChangeMe!_1234'

# ----------------------------------------------------------------
# Plugin list (optional)
# - Comma-separated OR space-separated both work.
# - These are Jenkins plugin IDs (not display names).
# - Feel free to edit/remove/extend this list.
#   Common basics: Git, Pipelines, Docker, Credentials, JCasC, Job DSL.
# ----------------------------------------------------------------
plugins='git,workflow-aggregator,docker-workflow,credentials-binding,configuration-as-code,job-dsl,blueocean'

# ----------------------------------------------------------------
# (Optional, future-proof) Variables you might want later.
# The current script does NOT read these—kept here for convenience
# if you extend the script to allow overrides from config.
# Uncomment and use only if you modify jenkins.sh to read them.
# ----------------------------------------------------------------

# Terraform version to install (script currently hardcodes 1.9.5)
# tf_version='1.9.5'

# Jenkins Docker image tag to use in Terraform (script uses jenkins/jenkins:lts)
# jenkins_image='jenkins/jenkins:lts'

# Host ports to map (script currently maps 8080 and 50000 1:1)
# http_port='8080'
# agent_port='50000'

# Extra Java options (script sets -Djenkins.install.runSetupWizard=false)
# java_opts='-Djenkins.install.runSetupWizard=false'

# Proxy settings for plugin installs (if your network requires it)
# http_proxy='http://proxy.example.com:8080'
# https_proxy='http://proxy.example.com:8443'

